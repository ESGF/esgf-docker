<?xml version="1.0" encoding="UTF-8"?>
<OPS>
    <OP>
        <!--
            The first entry is blank to indicate to the user that they can type in their
            own if needed
        -->
        <NAME></NAME>
        <URL>https://</URL>
    </OP>

    {{- range .Values.config.knownProviders }}
    <OP>
        <NAME>{{ .name }}</NAME>
        <URL>{{ .url }}</URL>
    </OP>
    {{- end }}
</OPS>
